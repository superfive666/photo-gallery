这是一个多轮评审中的重新生成请求。用户对部分镜头不满意，请**只重写这些镜头**的
描述与检索 query。已锁定的镜头绝对不要改动，也不要出现在输出里。

原始剧本：
<<<
{{script}}
>>>

历史轮次（按时间顺序，含每轮的镜头描述、检索 query 与用户反馈）：
{{history}}

本轮用户的项目级补充意见（累积）：
{{notes}}

需要重写的镜头及其反馈：
{{targets}}

要求：
1. 新的描述与 query 必须与上一轮**明显不同**（上一轮的候选已被用户否决，
   换一个角度描述画面，而不是换个说法复读）。
2. 结合用户反馈里「想要什么、不要什么」的信息。
3. 保持与已锁定镜头的整体风格一致。

输出 JSON：
{
  "shots": [
    { "idx": 3, "description": "新的具象画面描述", "queries": ["query 1", "query 2"] }
  ]
}
